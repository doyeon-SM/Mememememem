/*using UnityEngine;

public class CookObject : MonoBehaviour, IInteractable
{

    public void Interact(PlayerInventory inventory)
    {
        if(UIStateManager.Instance != null)
        {
            UIStateManager.Instance.OpenUI(UIType.Cooking);
        }
    }
    public void UIClose()
    {
        if (UIStateManager.Instance != null)
        {
            UIStateManager.Instance.CloseUI(UIType.Cooking);
        }
    }
}
*/